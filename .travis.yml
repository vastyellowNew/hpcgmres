language: cpp

sudo: required

compiler:
  - gcc
  - clang

script:
  - git clone https://github.com/giaf/blasfeo.git
  - cd blasfeo
  - make static_library -j $(nproc) TARGET=GENERIC
  - cd ..
  - cd unittest
  - mkdir -p build 
  - cd build
  - cmake ..
  - make
  - ctest 